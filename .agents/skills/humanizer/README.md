# Humanizer

A skill for Claude Code and OpenCode that removes signs of AI-generated writing from text, making it sound more natural and human.

This project-local copy is used inside `/Users/xuwenjing/Documents/topic-workflow` so the workflow can call the same humanizing instructions without depending on a personal global plugin path.
